// function expression
const divide = function(a, b){
    return a / b;
}
//arrow function
const square = (x) => x*x;

//arrow function
const add = (a, b) => a + b;

module.exports = {
    divide,
    square,
    add,
};